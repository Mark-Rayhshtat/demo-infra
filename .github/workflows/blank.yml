name: Hello World
on: [push]
jobs:
  Hello-World-Job:
    runs-on:
      - codebuild-mark-test-${{ github.run_id }}-${{ github.run_attempt }}
    # steps:
    #   - run: echo "Hello World"
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v6
        with:
          aws-region: eu-west-1
          role-to-assume: arn:aws:iam::581349712378:role/role-mark-test

      - name: sts
        run: |
          aws sts get-caller-identity
